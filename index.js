// Declare variable 
var katzDeliLine = [];

// create a function takeANumber

function takeANumber(katzDeliLine, person) {
  katzDeliLine.push(person);
  return "Welcome, " + person + ". You are number " + person + " in line."
}